import styled from "styled-components";

export const MainContent = () => {
    return (
        <StyledContent>
                <StyledMainTitle>I'm Chen Viktoria</StyledMainTitle>
                <StyledName>Frontend Developer</StyledName>
                <p>Completed a year-long frontend program at IT Incubator, plus a six-month internship at another company on the school's recommendation. Looking for a frontend developer position where I can keep growing.</p>
        </StyledContent>
    );
};

const StyledContent = styled.div`

`

const StyledMainTitle = styled.h1`
    
`

const StyledName = styled.h2`
    
`